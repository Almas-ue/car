import React from "react";
import ReactDOM from "react-dom/client";
import Car from "./block-element/Car.jsx";
import "normalize.css";
import "./index.scss";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Car />
  </React.StrictMode>
);
